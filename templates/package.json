{
  "name": "<%= appName %>",
  "version": "<%= appVersion %>",
  "description": "<%= appDescription %>",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>"
  },
  "main": "index.js",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-appfy-tasks": "git+https://git@github.com/geut/gulp-appfy-tasks.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
